fun add(left: int, right: int) -> int {
    left + right
}

fun complex(n: int) -> bool {
    val m: int = m * m;
    val res: bool = true;
    while m > n {
        res = !res;
        m /= add(1, 1);
    }

    res
}

fun main(n: int) {
    return complex(n + n)
}