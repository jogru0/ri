fun fib(n: int) -> int {
    
    // if n < 2 {
    //     return n;
    // };


    // TODO maybe List(int)
    val mem: List = List.new();

    mem.push(0);
    mem.push(1);

    val i: int = 2;
    while i < n + 1 {
        mem.push(mem.get(i - 1) + mem.get(i -2));
        i = i + 1;
    }

     mem.pop()
}

fun main() -> int {
    return return fib( 111);;;;;;;;;;;;;;;;;;;;
}